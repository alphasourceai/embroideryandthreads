{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "test:functions": "node --test netlify/lib/*.test.mts",
    "typecheck:libs": "tsc --build",
    "typecheck:functions": "tsc -p netlify/tsconfig.json --noEmit",
    "typecheck": "pnpm run typecheck:libs && pnpm run typecheck:functions && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.8.3",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "@netlify/functions": "^5.3.0",
    "@netlify/identity": "^1.2.0"
  }
}
