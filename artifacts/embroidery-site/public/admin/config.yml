backend:
  name: git-gateway
  branch: main
  identity_url: https://embroideryandthreads.netlify.app/.netlify/identity
  gateway_url: https://embroideryandthreads.netlify.app/.netlify/git/github

site_url: https://embroideryandthreads.com
display_url: https://embroideryandthreads.com
logo_url: https://embroideryandthreads.com/logo-b.jpg
media_folder: artifacts/embroidery-site/public/uploads
public_folder: /uploads
publish_mode: simple

collections:
  - label: Website
    name: website
    editor:
      preview: false
    files:
      - label: Page Photos
        name: site_content
        file: artifacts/embroidery-site/src/content/site.json
        format: json
        fields:
          - label: Hero Image
            name: hero
            widget: object
            fields:
              - { label: Image, name: image, widget: image, hint: "Drag a replacement image here or choose one from the media library." }
              - { label: Image Description, name: alt, widget: string, hint: "Describe what is visible for accessibility and search." }
          - label: Story Image
            name: story
            widget: object
            fields:
              - { label: Image, name: image, widget: image }
              - { label: Image Description, name: alt, widget: string }
          - label: Gallery
            name: gallery
            widget: list
            min: 1
            summary: "{{fields.alt}}"
            fields:
              - { label: Image, name: image, widget: image }
              - { label: Image Description, name: alt, widget: string }
          # These values remain in site.json but are intentionally unavailable
          # in the client editor. Restore the pricing object documented in
          # docs/client-site-editor.md when the public pricing section is ready.
          - { label: Customer Stories, name: reviews, widget: hidden }
          - { label: Instagram Highlights, name: instagram, widget: hidden }
          - { label: Pricing, name: pricing, widget: hidden }
